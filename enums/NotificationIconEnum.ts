export enum NotificationIconEnum {
  WATER = 'water',
  LIGHT = 'light',
  TEMPERATURE = 'temperature',
  NEW_PLANT = 'new-plant',
}

export enum NotificationTypeEnum {
  NEW_PLANT = 'NEW_PLANT',
  WATERING = 'WATERING',
}
